# 多智能体天气出行助手 — 面试题库与参考答案

> 基于 `weather-travel-agent` 项目，由资深 AI Agent 岗位面试官出题，覆盖从架构设计到工程落地全链路。

---

## 一、Agent 架构与多智能体协作

### Q1. 这个项目采用了多 Agent 架构，请画出架构图并说明每个 Agent 的职责？为什么选择 4 个 Agent 而不是 1 个大 Agent？

**参考答案：**

架构图：

```
用户问题 ──► IntentAgent（意图识别 + 城市/偏好抽取）
                  │
         ┌───────┼────────┐
    weather/travel       chat
         │                  │
         ▼                  ▼
   WeatherAgent        ChatAgent
  (BatchNode, MCP协议)  (兜底闲聊)
         │
         ▼
   AdviceAgent
  (RAG + 长期记忆 → 个性化建议)
```

4 个 Agent 的职责：

| Agent | 职责 | 技术特点 |
|-------|------|---------|
| **IntentAgent** | 意图分类 + 实体抽取（城市、偏好标签） | LLM 结构化 JSON 抽取 |
| **WeatherAgent** | 通过 MCP 协议获取多城市实时天气 | BatchNode 并行执行 |
| **AdviceAgent** | 综合天气+知识库+用户画像生成建议 | RAG + 长期记忆融合 |
| **ChatAgent** | 非天气类问题兜底回复 | 简单路由分支 |

**为什么是 4 个 Agent 而非 1 个大 Agent：**

1. **关注点分离（Separation of Concerns）**：意图识别、工具调用、知识融合、闲聊兜底各有独立职责，单一 Agent 容易出现 prompt 臃肿、边界模糊的问题。
2. **可独立迭代与评测**：每个 Agent 可独立优化 prompt、独立评测，不会互相耦合。例如可以单独提升意图识别准确率而不影响天气生成逻辑。
3. **并行执行**：WeatherAgent 继承 `BatchNode`，支持多城市并行查询天气——单 Agent 模式很难做到这种细粒度的并行控制。
4. **路由灵活性**：通过 IntentAgent 的返回值控制有向图的路由（`weather/travel → WeatherAgent`，`chat → ChatAgent`），不同意图走不同处理链路，避免无效计算。
5. **面试价值**：真实 Agent 工程师岗位要求的就是这种多 Agent 协作能力，单一巨型 Agent 是 toy 项目的特征。

---

### Q2. `weather` 和 `travel` 两个意图都路由到了 WeatherAgent，这个设计合理吗？什么场景下应该分叉？

**参考答案：**

**合理性分析：**

这是合理且务实的设计。原因：
- `weather`（纯天气查询）和 `travel`（出行建议）都需要天气数据作为输入，共享 WeatherAgent 避免重复查询。
- 两者的差异体现在 AdviceAgent 的行为上：`travel` 意图携带用户偏好标签（如"带宝宝"），AdviceAgent 会结合偏好生成更有针对性的建议；而纯 `weather` 只生成基础天气总结。
- 这也是"关注点分离"的体现：数据获取（WeatherAgent）与建议生成（AdviceAgent）解耦。

**什么场景下应该分叉：**

如果 `weather` 和 `travel` 在数据获取阶段就有本质差异，就应该分叉。例如：
- `travel` 需要额外查询：紫外线指数、空气质量、景点开放时间 → 应走独立的 TravelDataAgent
- `weather` 需要查询未来 7 天预报 → 应走 ForecastAgent
- 两者调用的 MCP Tool 列表不同 → 分叉更清晰

当前项目保持简单合并是合理的，但面试中可以主动讨论这个扩展思路。

---

### Q3. PocketFlow 框架中 `Node` 的三个方法 `prep` → `exec` → `post` 是如何工作的？`post` 的返回值有什么特殊含义？

**参考答案：**

PocketFlow 的 `Node` 遵循 **三段式生命周期**：

```python
class IntentAgent(Node):
    def prep(self, shared):    # ① 准备阶段：从 shared 读取输入
        return shared["question"]

    def exec(self, question):  # ② 执行阶段：纯计算，无副作用
        return complete_json(prompt)

    def post(self, shared, prep_res, exec_res):  # ③ 后处理：写回 shared + 决定路由
        shared["intent"] = exec_res
        return exec_res.get("action", "chat")  # ← 返回值是关键
```

**三个阶段的设计哲学：**

| 阶段 | 输入 | 职责 | 特点 |
|------|------|------|------|
| `prep` | `shared` 全局状态 | 从共享状态提取需要的数据 | 有副作用（可读 shared） |
| `exec` | `prep` 的返回值 | 纯计算逻辑 | **无副作用**，可并行、可重试 |
| `post` | `prep_res` + `exec_res` | 结果写回 shared + 控制路由 | 有副作用（写 shared） |

**`post` 返回值的特殊含义：**

`post` 的返回值作为 **action 字符串**，驱动 PocketFlow 的有向图路由：

```python
intent - "weather" >> weather   # post 返回 "weather" 时跳转到 weather
intent - "travel" >> weather    # post 返回 "travel" 时跳转到 weather
intent - "chat" >> chat         # post 返回 "chat" 时跳转到 chat
weather - "advice" >> advice    # post 返回 "advice" 时跳转到 advice
```

这是 PocketFlow 的**条件边（Conditional Edge）**机制，比 LangGraph 的 `add_conditional_edges` 更简洁。

特别地，`BatchNode` 的 `exec` 会被并行调用多次（每个城市一次），`post` 汇总所有并行结果。

---

## 二、MCP 协议与工具调用

### Q4. 你手写了一个 MCP stdio 服务器，请解释 MCP 协议的核心流程，以及为什么不用现成的 MCP SDK？

**参考答案：**

**MCP 协议核心流程（本项目的实现）：**

```
MCP Client (mcp_client.py)          MCP Server (weather_mcp_server.py)
        │                                       │
        │──── subprocess.Popen() ──────────────►│  启动子进程
        │                                       │
        │──── initialize (JSON-RPC) ───────────►│  握手：协议版本 2024-11-05
        │◄─── capabilities: {tools: {}} ────────│
        │                                       │
        │──── notifications/initialized ───────►│  通知：初始化完成
        │                                       │
        │──── tools/list ──────────────────────►│  动态发现工具
        │◄─── [{"name":"get_weather",...}] ────│  返回工具列表 + schema
        │                                       │
        │──── tools/call {"name":"get_weather", │  调用工具
        │      "arguments":{"city":"北京"}} ───►│
        │◄─── {"content":[{"type":"text",...}]}─│  返回结果（天气 JSON）
```

**通信方式：** stdio（标准输入/输出），每行一个 JSON-RPC 2.0 消息。

**为什么不用现成 MCP SDK：**

1. **面试核武器**：面试官看到"手写 MCP 协议实现"直接知道你不是只会 `pip install`，而是真正理解了协议——JSON-RPC 2.0 + stdio 传输 + `tools/list` + `tools/call` 的核心链路。
2. **无依赖/可审计**：不引入第三方 MCP SDK（如 `mcp`、`anthropic-sdk`），代码完全透明，面试官可以一次阅读完整个协议实现。
3. **体积最小化**：`mcp_client.py` ~70 行，`weather_mcp_server.py` ~75 行，极简实现证明了对协议的深刻理解。
4. **教学价值**：手写一遍才能真正理解 `initialize` → `tools/list` → `tools/call` 的握手-发现-调用流程，这也是面试中被追问的核心。

当然，**生产环境**还是建议用 SDK，因为有鉴权、会话管理、错误恢复等成熟能力。面试中要强调"知道何时手写何时用 SDK"。

---

### Q5. WeatherAgent 是如何通过 MCP 协议获取天气的？请走一遍完整调用链。

**参考答案：**

完整调用链：

**第 1 步：WeatherAgent.prep() — 初始化 MCP 客户端**

```python
def prep(self, shared):
    self.client = MCPWeatherClient()  # 启动 MCP 服务器子进程
    return shared.get("cities", [])   # 返回城市列表供 BatchNode 并行
```

`MCPWeatherClient.__init__()` 内部：
- `subprocess.Popen` 启动 `weather_mcp_server.py` 子进程
- 发送 `initialize` JSON-RPC 请求完成握手
- 发送 `notifications/initialized` 通知

**第 2 步：WeatherAgent.exec() — 并行调用（BatchNode）**

```python
def exec(self, city):
    return self.client.get_weather(city)  # 每个城市在独立线程中执行
```

`MCPWeatherClient.get_weather(city)` 内部：
1. 构造 `tools/call` JSON-RPC 请求：`{"jsonrpc":"2.0","method":"tools/call","params":{"name":"get_weather","arguments":{"city":"北京"}}}`
2. 通过 stdin 发送给 MCP Server 子进程
3. 从 stdout 逐行读取，匹配 `id` 字段获取响应
4. 解析响应中的 `result.content[0].text`（JSON 字符串）返回天气数据

**第 3 步：MCP Server 处理**

`weather_mcp_server.py` 的 `handle()` 函数：
1. 匹配 `method == "tools/call"` 
2. 提取 `params.name` 和 `params.arguments`
3. 调用 `get_weather(args.get("city", ""))` → 实际调用 `utils/weather_client.py` 的 Open-Meteo API
4. 将天气数据封装为 MCP 标准响应格式：`{"result": {"content": [{"type":"text", "text": "..."}]}}`

**第 4 步：WeatherAgent.post() — 汇总与记忆写入**

```python
def post(self, shared, prep_res, exec_res_list):
    self.client.close()                       # 关闭 MCP 子进程
    shared["weather_data"] = exec_res_list    # 汇总所有并行结果
    for i, city in enumerate(shared.get("cities", [])):
        if not wd.get("fallback"):
            record_query(city, shared.get("preferences", []))  # 写入长期记忆
    return "advice"  # 路由到 AdviceAgent
```

**关键设计亮点：**
- BatchNode 自动并行：多个城市同时调用 MCP，而非串行
- 一次流程只启一个 MCP 子进程（`prep` 创建，`post` 关闭），跨城市复用
- 只在成功获取天气时写入长期记忆（`fallback` 数据不写）

---

### Q6. `tools/list` 在这个项目中是否实际被调用了？它的设计意义是什么？

**参考答案：**

在本项目的 MCP Client 中，`tools/list` 方法已经实现但未在 WeatherAgent 中显式调用——因为当前只有一个 Tool (`get_weather`)，Agent 知道需要调用什么。

但它的**设计意义**非常重要：

1. **动态工具发现（核心价值）**：在真实 MCP 生态中，Agent 不应硬编码知道有哪些工具。通过 `tools/list` 获取工具列表及其 `inputSchema`，LLM 可以动态决定调用哪个工具、传什么参数——这就是 **Function Calling 的标准协议**。

2. **未来扩展的接口**：当 MCP Server 新增更多工具（如 `get_uv_index`、`get_air_quality`），Agent 通过 `tools/list` 自动感知，无需修改代码。

3. **与实际 Function Calling 的对接**：`tools/list` 返回的 `inputSchema`（OpenAPI/JSON Schema 格式）可以直接注入到 LLM 的 `tools` 参数中，实现标准化的 Function Calling。

面试中可以说："当前项目因为工具固定所以没显式调 `tools/list`，但在完整实现中，会在 IntentAgent 之前调用 `tools/list`，将工具列表注入 LLM prompt，让 LLM 自主选择调用哪个工具——这才是 MCP 的完整用法。"

---

## 三、RAG 检索增强生成

### Q7. 你的 RAG 系统是如何设计的？为什么选择 BGE-M3 + 纯 Python 余弦相似度而不是用 FAISS/向量数据库？

**参考答案：**

**整体架构：**

```
用户查询 + 天气数据 + 偏好
        │
        ▼
  KnowledgeBase.retrieve(query, k=3)
        │
   ┌────┴────┐
   │         │
  有 API Key    无 API Key
   │              │
   ▼              ▼
 BGE-M3 Embedding   中文 Bigram 关键词匹配
 + 纯 Python 余弦     + 重叠分打分
        │              │
        └────┬─────────┘
             ▼
       Top-K 知识条目
             │
             ▼
      注入 AdviceAgent 的 prompt
```

**为什么选择 BGE-M3：**

1. **中文友好**：BAAI/bge-m3 是目前中文 embedding 的 SOTA 之一，对中文语义理解远超通用多语言模型。
2. **免费接入**：通过 SiliconFlow API 调用，无需自己部署 GPU。
3. **多粒度支持**：bge-m3 支持 dense + sparse + colbert 三种检索方式，本项目用 dense（8192 维向量）。

**为什么纯 Python 余弦而非 FAISS/向量数据库：**

| 决策因素 | 分析 |
|----------|------|
| **语料规模** | 仅 17 条知识条目，暴力遍历 < 1ms |
| **减少依赖** | 不引入 numpy/faiss/chromadb，`requirements.txt` 只有 4 行 |
| **可读性** | 面试官可以直接看懂余弦相似度的纯 Python 实现，不会被框架遮蔽 |
| **教育意义** | 手写余弦证明你理解向量检索的数学原理 |

**明确的权衡声明（面试加分项）**：

> "当前方案适用于小规模语料的教学/原型场景。如果语料达到 10 万+ 条，需要引入 FAISS（IVF 索引）或向量数据库（Milvus/ChromaDB）。同时中文 Bigram 分词方案在长尾查询上召回率会下降，需要混合检索（sparse + dense）。我在设计时保留了这些扩展点。"

---

### Q8. 你的知识库语料有 18 条，覆盖了哪些场景？如果让你扩展这个知识库，你会怎么做？

**参考答案：**

**当前语料覆盖的场景：**

| 类别 | 条目数 | 示例 |
|------|--------|------|
| 天气类（降雨/雷暴/冰雪/高温/寒冷/大风/雾霾） | 7 条 | 7 种极端天气的应对建议 |
| 场景类（亲子/骑行/通勤/防晒/过敏） | 5 条 | 特定出行场景的专业建议 |
| 城市类（北京/上海/广州/深圳/哈尔滨/成都） | 6 条 | 城市气候特点与季节建议 |

**扩展方案（体现系统性思维）：**

**1. 数据来源扩展**
- 接入公开气象知识库（如中国气象局科普资料）
- 从旅游攻略平台（马蜂窝、小红书）爬取城市出行建议
- 利用 LLM 生成高质量合成语料（synthetic data generation），然后人工校验

**2. 检索策略升级**
- 引入**混合检索（Hybrid Search）**：BM25 稀疏检索 + BGE-M3 稠密检索，加权融合
- 增加**重排序（Reranker）**：用 bge-reranker-v2 对初检结果精排
- 支持**元数据过滤**：按 `tag`（如"亲子"、"城市-北京"）预过滤再检索

**3. 知识库管理**
- 引入**增量更新**：支持热加载新知识，无需重启服务
- 增加**知识过期机制**：城市季节性建议有时效性，需要定期更新
- 建立**知识质量评测**：用 eval 集定期测检索命中率，做回归检测

---

### Q9. 检索结果中出现了不相关知识时，你的系统会怎么处理？LLM 是否会被误导？

**参考答案：**

当前项目的**防御机制：**

1. **Top-K 截断 + 回退机制**（`knowledge_base.py` 第 65-66 行）：
   ```python
   top = [self.docs[i] for _, i in scored[:k] if _ > 0]  # 重叠分 > 0 才入选
   return top or self.docs[:k]  # 全为 0 时回退取前 k 条
   ```
   关键词模式下，重叠分为 0 的知识不会被选中，最坏情况回退取前 k 条兜底。

2. **Prompt 约束**（`advice_agent.py` 第 41 行）：
   > "不要编造天气数据，基于天气数据作答"
   
   这条约束告诉 LLM：知识库内容是参考而非权威，优先以天气数据为准。

3. **LLM 本身的鲁棒性**：DeepSeek 等大模型对不相关的上下文有一定过滤能力，不会盲目采信。

**改进方案（面试中可以主动提出）：**

- **相关性阈值过滤**：设定余弦相似度阈值（如 0.6），低于阈值不注入 prompt
- **Reranker 二次过滤**：用专门的 cross-encoder 模型对 Top-K 结果重新打分
- **引用溯源**：在回答中标注每条建议的知识来源，让用户自行判断可靠性
- **RAGAS 评测**：用 RAGAS 框架（faithfulness + relevance + precision）持续监控检索质量

---

## 四、长期记忆与用户画像

### Q10. 你的长期记忆系统是"双轨存储"，请解释设计思路，以及它与对话上下文的区别？

**参考答案：**

**双轨存储架构：**

```json
{
  "cities": {           // 轨1: 城市查询频次统计
    "北京": 5,
    "深圳": 3
  },
  "preferences": [      // 轨2: 偏好标签去重列表
    "带宝宝",
    "骑行"
  ],
  "history": [          // 辅助: 最近30次查询记录
    "北京", "北京", "深圳", "北京", ...
  ]
}
```

**双轨 vs 对话上下文：**

| 维度 | 对话上下文 | 长期记忆（本项目） |
|------|-----------|-------------------|
| 生命周期 | 单次对话 / Session | 跨会话持久化（JSON 文件） |
| 内容 | 本轮问答原文 | 聚合后的用户画像（频次 + 标签） |
| 更新方式 | 每次对话临时拼接 | 每次查询逐步累加 |
| 用途 | 上下文推理 | 跨会话个性化 |
| 例子 | "刚才你问了北京天气" | "你常关注北京、带宝宝出行" |

**核心设计思路：**

1. **无感记忆**：用户不需要显式设置偏好，系统从对话中自动抽取标签（`IntentAgent` 的 `preferences` 字段），下次自动复用到 AdviceAgent 的 prompt 中。

2. **去重合并**（`advice_agent.py` 第 28 行）：
   ```python
   merged_prefs = list(dict.fromkeys(prefs + profile.get("preferences", [])))
   ```
   当次对话的偏好与历史偏好合并去重，避免重复。

3. **可量化**：`repeat_rate()` 函数（`user_profile.py`）计算同一会话内重复查询占比，作为记忆有效性的量化指标——数值越低说明记忆帮用户省去了重复询问。

4. **遗忘机制**：history 最多保留 30 条（FIFO），防止无限制增长。

---

### Q11. 如果用户偏好发生变化（比如以前带宝宝，现在孩子长大了不再需要），你的系统如何处理？

**参考答案：**

坦诚地说，当前实现**没有偏好衰减/遗忘机制**，这是一个已知局限。但面试中主动指出并给出解决方案，反而加分。

**当前问题：**
- 偏好标签一旦加入 `preferences` 列表就是永久的
- 用户无法手动修改或删除偏好
- 系统不会根据时间衰减旧偏好

**解决方案（三选一或组合）：**

**方案 A：时间衰减权重**
```python
# 每条偏好记录加入时间戳，近期偏好权重高
preferences = [
    {"tag": "带宝宝", "last_seen": "2026-08-10", "count": 5},
    {"tag": "骑行",   "last_seen": "2025-03-01", "count": 2},  # 半年没出现，权重降低
]
# 注入 prompt 时只取近 30 天活跃的偏好
```

**方案 B：显式偏好管理**
- 增加 `/preferences` API 端点，用户可查看/删除偏好
- 在 ChatAgent 中识别"我不需要带宝宝的建议了"等意图，触发偏好清理

**方案 C：LLM 动态判断**
- 在每次生成时将历史偏好提供给 LLM
- Prompt 中加入："以下为用户历史偏好，请根据当前对话判断哪些仍然适用"
- 让 LLM 自行筛选（消耗 token 但最灵活）

面试回答模板："目前的 MVP 版本偏好是永久积累的，我认识到这是产品化的短板。改进方向是加入时间衰减 + 用户可管理 + LLM 动态筛选的组合方案。"

---

## 五、LLM 集成与优雅降级

### Q12. 你的 `utils/llm.py` 实现了一套"优雅降级"机制，请解释其设计思路和回退逻辑。

**参考答案：**

**设计哲学：Agent 永远不因缺少 API Key 而崩溃。**

```
有 DEEPSEEK_API_KEY ──► 调用 DeepSeek chat API (真实 LLM)
                              │
无 DEEPSEEK_API_KEY ──► 本地启发式 mock
                              │
                     ┌────────┴────────┐
                     │                 │
               意图分类 prompt      建议生成 prompt
                     │                 │
                     ▼                 ▼
          正则匹配城市+偏好标签   正则解析天气数据行
          规则判断 action        规则生成中文建议
```

**回退逻辑的分支判断**（`utils/llm.py` 第 91-97 行）：

```python
def chat(messages):
    if USE_REAL_LLM:
        return _real_chat(messages)   # 走 DeepSeek
    
    # 无 Key 时的回退
    last = messages[-1]["content"]
    if "action" in last and ("天气" in last or "建议" in last):
        return json.dumps(_mock_intent(last))  # 意图分类回退
    return _mock_answer(last)                  # 建议生成回退
```

**两个回退函数的设计：**

| 回退函数 | 输入 | 核心逻辑 | 输出 |
|---------|------|---------|------|
| `_mock_intent` | 意图分类 prompt | 正则提取城市名 + 关键词匹配意图 + 偏好标签识别 | `{"action":"travel","cities":["北京"],"preferences":["带宝宝"]}` |
| `_mock_answer` | 建议生成 prompt | 正则解析天气数据行 → 根据温度/天气状况匹配规则 → 拼接中文建议 | 结构化的城市天气建议 |

**工程价值：**

1. **可演示**：面试现场无需联网，直接 `python main.py` 即可跑通全链路
2. **可评测**：`eval/run_eval.py` 无 Key 也能运行，验证路由和检索逻辑
3. **CI/CD 友好**：单元测试不依赖外部 API，不花钱、不 flaky
4. **渐进式启用**：加上 Key 就自动升级为真实 LLM，零代码修改

---

### Q13. DeepSeek API 调用失败时你的系统会怎样？有没有考虑过 LLM 级别的容错？

**参考答案：**

**当前状态：**

`utils/llm.py` 的 `_real_chat()` 函数没有 try-except 包裹，DeepSeek API 调用失败会直接抛异常。这个问题在 `get_weather()` 中有成熟处理（重试+兜底），但 LLM 调用层面确实缺这层保护。

**改进方案（面试中主动提出）：**

```python
def _real_chat(messages, retries=3):
    for attempt in range(retries):
        try:
            client = OpenAI(api_key=DEEPSEEK_API_KEY, base_url="https://api.deepseek.com")
            resp = client.chat.completions.create(
                model="deepseek-chat",
                messages=messages,
                temperature=0.3,
                timeout=15,  # 加入超时
            )
            return resp.choices[0].message.content.strip()
        except Exception as e:
            if attempt == retries - 1:
                # 最终回退到 mock
                return _mock_answer(messages[-1]["content"])
            time.sleep(1 * (attempt + 1))  # 指数退避
```

**多级容错策略：**

| 层级 | 策略 | 实现位置 |
|------|------|---------|
| 1. 重试 | 3 次重试 + 指数退避 | LLM 调用层 |
| 2. 超时 | 设置 timeout，防止挂死 | OpenAI client |
| 3. 降级 | 降级到 mock 启发式 | 最终回退 |
| 4. 熔断 | 连续 N 次失败暂时跳过 LLM | 需额外实现 |
| 5. 备用模型 | 切换到备用 LLM（如 Qwen） | 需额外实现 |

**面试强调点：**

> "`weather_client.py` 的四重保护（重试/超时/限流/兜底）是完整且成熟的，LLM 调用层我应该也加上同样的保护。核心原则是：**Agent 链路中的任何一个节点都不应该是单点故障源**。"

---

## 六、评测体系

### Q14. 你设计了一套完整的评测指标，请逐一解释每个指标的含义及其为什么重要。

**参考答案：**

**评测指标体系：**

| 指标 | 本项目值 | 含义 | 为什么重要 |
|------|---------|------|-----------|
| **task_success_rate** | 100% | 任务成功率：返回有效回答且无兜底失败 | **核心指标**：用户最终体验，低于 95% 不可上线 |
| **intent_accuracy** | 90% | 意图识别准确率：pred_action == expected_action | 意图错了后面全错，**路由正确性是系统基石** |
| **task_routing_accuracy** | 100% | 任务路由准确率：闲聊 vs 任务二分 | 比细粒度准确率更重要——把"聊天"误路由到"天气"比反过来更严重 |
| **avg_latency_s** | ~7.0s | 平均端到端时延 | 用户体验指标，高于 10s 需要优化（缓存/预取/流式） |
| **weather_fallback_rate** | 0% | 天气工具兜底率 | 越低说明基础设施越稳定；高兜底率意味着频繁返回假数据 |
| **retrieval_accuracy** | 100% | RAG 检索命中率：检索结果覆盖预期知识标签 | 衡量知识库是否真正被用起来了 |
| **repeat_question_rate** | 可计算 | 重复提问率：同会话内重复问同城市占比 | **长期记忆有效性的代理指标**：越低说明记忆帮用户省去了重复 |

**设计亮点：**

1. **分层评测**：不是只测最终答案，而是拆解到意图→路由→工具→检索→记忆每个环节
2. **代理指标（Proxy Metrics）**：`repeat_question_rate` 作为记忆生效的量化代理，不需要依赖用户问卷
3. **自动化回归**：`eval/run_eval.py` 跑完自动输出 `metrics.json`，可直接接入 CI

---

### Q15. 你的 eval_set.json 只有 10 条用例，这够吗？如果让你扩展评测体系，你会怎么做？

**参考答案：**

坦诚地说，10 条太少——覆盖了 happy path 但缺少 edge cases。

**当前 10 条用例覆盖：**

| 类型 | 条数 | 示例 |
|------|------|------|
| 单城市天气 | 3 | 北京、哈尔滨、深圳 |
| 出行/旅行 | 4 | 带宝宝、梅雨季、台风、骑行 |
| 多城市对比 | 1 | 北京和成都对比 |
| 闲聊 | 1 | 你好啊 |
| 复合场景 | 1 | 高温+通勤 |

**扩展方案：**

**1. 增加边界用例**
```json
{"question": "", "expected_action": "chat"},                             // 空输入
{"question": "abc123", "expected_action": "chat"},                       // 乱码
{"question": "火星上的天气", "expected_action": "weather"},              // 不存在的城市
{"question": "我今天心情不好", "expected_action": "chat"},               // 情绪表达
{"question": "北京 1990 年 1 月 1 日天气", "expected_action": "weather"}, // 超出能力范围
```

**2. 建立分层评测体系**
- **单元测试**：每个 Agent 的 `exec` 方法独立测（需 mock LLM）
- **集成测试**：测试 Agent 间路由正确性
- **端到端测试**：当前 `eval_set.json` 做的事
- **人工评测**：随机抽样 50 条，人工打分（1-5 分）可用性与准确性

**3. 引入自动评估模型**
- **LLM-as-Judge**：用 GPT-4/DeepSeek 自动给回答打分，评估准确性、流畅性、建议的可执行性
- **RAGAS**：专门评测 RAG 的框架（faithfulness, answer_relevancy, context_precision, context_recall）

**4. 在线指标**
- 用户点赞/点踩率
- 二次追问率（第一次回答不满意需要追问的比例）
- 平均对话轮次

---

## 七、工程化与容错

### Q16. `utils/weather_client.py` 实现了四重保护，请逐一解释并说明为什么每重都必要。

**参考答案：**

```python
def get_weather(city, retries=3, timeout=8):
```

**四重保护机制：**

| 保护层 | 实现 | 代码位置 | 解决问题 |
|--------|------|---------|---------|
| **① 重试 (retry)** | `for attempt in range(retries):` | 第 43 行 | 网络抖动、临时故障 |
| **② 超时 (timeout)** | `urllib.request.urlopen(req, timeout=timeout)` | 第 36 行 | API 响应慢/挂死，防止链路阻塞 |
| **③ 限流 (rate limit)** | `_MIN_INTERVAL = 0.3`，两次调用至少间隔 0.3s | 第 46-48 行 | 保护上游 API（免费 API 通常有频率限制） |
| **④ 兜底 (fallback)** | 所有重试失败后返回占位数据 + `"fallback": True` | 第 77-80 行 | **保证 Agent 链路永不中断** |

**为什么每重都必要：**

1. **重试**：Open-Meteo 是免费 API，偶有波动，重试解决 90% 的临时故障
2. **超时**：如果 API 挂了（服务端 hang），没有超时会导致整个 Agent 请求永久挂起——这是生产事故
3. **限流**：BatchNode 并行时多个城市几乎同时请求，不加限流可能触发 API 的 rate limit 被 ban
4. **兜底**：这是最关键的一层——Agent 不能因为天气预报挂了就说"我不知道"。返回 `fallback: True` 的占位数据，AdviceAgent 看到后会生成保守建议（如"建议出行前关注最新天气预报"）

**兜底的消费方感知**：

```python
# weather_agent.py 第 25-26 行
if not wd.get("fallback"):
    record_query(city, shared.get("preferences", []))  # 兜底数据不写入记忆
```

兜底数据不会被写入长期记忆，避免污染用户画像。

---

### Q17. 为什么 WeatherAgent 使用 `BatchNode` 而不是 `Node`？BatchNode 的并行是如何实现的？

**参考答案：**

**为什么用 BatchNode：**

如果用户问"北京、上海、深圳、广州四个城市天气怎么样"：

| 方案 | 耗时 | 特点 |
|------|------|------|
| Node 串行 | 4 × 2s = 8s | 简单，但慢 |
| BatchNode 并行 | max(2s) ≈ 2s | **4 倍加速** |

`BatchNode` 的 `prep` 返回一个列表，PocketFlow 框架内部用 **ThreadPoolExecutor** 对列表中每个元素并行调用 `exec`。

```python
class WeatherAgent(BatchNode):
    def prep(self, shared):
        return shared.get("cities", [])    # 返回 ["北京", "上海", "深圳", "广州"]

    def exec(self, city):                   # 4 个线程并行执行
        return self.client.get_weather(city)  # 每个线程走 MCP 子进程
```

**"线程安全"问题的处理：**

`MCPWeatherClient` 使用了 `subprocess.Popen` 启动子进程，而 **stdin/stdout 不是线程安全的**。但本项目中**一个 WeatherAgent 只创建一个 MCP 子进程**，多个线程共享同一个 stdin/stdout 写入和读取，存在竞争风险。

这是当前实现的一个技术债务。改进方案：
- 每个线程创建独立的 MCP 子进程（增加开销但安全）
- 或使用 asyncio + 协程替代多线程
- 或在 MCP Client 内部加锁保护 stdin/stdout

面试中主动指出这个权衡，展示系统思维。

---

## 八、部署与生产化

### Q18. 项目的 FastAPI 服务中，`flow` 是单例，这在并发场景下有什么问题？如何改进？

**参考答案：**

**当前代码（`api/app.py` 第 22 行）：**

```python
flow = create_weather_travel_flow()  # 模块级单例

@app.post("/chat")
def chat(req: ChatReq):
    shared = {"question": req.question}
    flow.run(shared)  # 多请求共享同一个 flow 实例
    return {"answer": shared.get("answer"), "intent": shared.get("intent")}
```

**并发问题：**

1. **`shared` 字典污染**：PocketFlow 的 `shared` 是 Agent 间共享状态，如果两个请求并发执行，可能有竞态条件。
2. **MCP 子进程冲突**：WeatherAgent 在 `prep` 中创建 MCP Client 子进程，多个请求可能创建多个子进程，资源泄漏。
3. **内存中的知识库单例**：`advice_agent.py` 第 10 行 `kb = KnowledgeBase()` 是模块级单例，如果未来支持热更新知识库会有问题。

**改进方案：**

**方案 A：每请求创建新 Flow（简单安全）**
```python
@app.post("/chat")
def chat(req: ChatReq):
    flow = create_weather_travel_flow()  # 每请求新建
    shared = {"question": req.question}
    flow.run(shared)
    return {"answer": shared.get("answer"), "intent": shared.get("intent")}
```

**方案 B：连接池模式（高性能）**
```python
from contextlib import contextmanager
from queue import Queue

flow_pool = Queue()
for _ in range(4):  # 4 个预创建实例
    flow_pool.put(create_weather_travel_flow())

@contextmanager
def get_flow():
    flow = flow_pool.get()
    try:
        yield flow
    finally:
        flow_pool.put(flow)

@app.post("/chat")
def chat(req: ChatReq):
    with get_flow() as flow:
        shared = {"question": req.question}
        flow.run(shared)
        return {"answer": shared.get("answer"), "intent": shared.get("intent")}
```

**方案 C：异步 + 无状态设计（最佳）**
- 将 MCP Client 改为异步 I/O
- 使用 `async def` 和 asyncio 协程
- 消除全局状态，每请求独立上下文

---

### Q19. Dockerfile 中 `context: ..` 的目的什么？为什么需要包含 PocketFlow？

**参考答案：**

```yaml
# docker-compose.yml
services:
  weather-agent:
    build:
      context: ..   # 以 01-Agent 目录为构建上下文
      dockerfile: weather-travel-agent/Dockerfile
```

**设计原因：**

项目目录结构是：
```
01-Agent/
├── PocketFlow/                 # Agent 框架源码（独立目录）
│   └── pocketflow/...
└── weather-travel-agent/       # 本项目
    ├── Dockerfile
    └── main.py
```

Docker 构建时，`COPY` 命令只能访问构建上下文（`context`）内的文件。如果 `context` 设在 `weather-travel-agent/`，则无法 `COPY ../PocketFlow`。

所以 `context: ..` 将构建上下文提升到 `01-Agent/`，使得 Dockerfile 中虽然没显式 COPY PocketFlow（注释掉了，实际用的是 `PYTHONPATH`），但原理上是为包含同级目录做准备。

**更好的做法（可在面试中提出）：**

- 将 PocketFlow 发布为 pip 包，在 `requirements.txt` 中引入，消除对本地目录的依赖
- 或使用 Git submodule + `COPY PocketFlow/ /app/PocketFlow/` 的方式明确引入

---

## 九、深度思考与开放性

### Q20. 如果让你把这个项目做成一个真正的商业产品（如面向家长的亲子出行助手小程序），你会做哪些改造？

**参考答案：**

**产品层面：**

| 维度 | 当前 MVP | 产品化改造 |
|------|---------|-----------|
| **输入** | 纯文本 | 语音输入 + 图像（拍照识别天气） |
| **输出** | 文本建议 | 卡片式 UI（天气图标 + 穿衣建议 + 出行评分）+ 推送通知 |
| **个性化** | 简单偏好标签 | 家庭成员画像（宝宝年龄/过敏史/常去地点） |
| **天气数据** | 仅实时天气 | 未来 7 天预报 + 紫外线 + 空气质量 + 降水概率 |
| **知识库** | 17 条静态语料 | 动态更新的亲子出行知识图谱（人-地点-活动-天气关联） |

**技术层面：**

1. **多模态输入**：支持用户拍照上传天空照片，用视觉模型分析云层→预测降雨概率
2. **流式输出（SSE/WebSocket）**：大模型回答逐字输出，降低用户等待焦虑
3. **多轮对话**：不是一问一答，而是类似 chat，可追问"那明天呢？""换个省钱的方案？"
4. **A/B 测试框架**：不同 prompt 模板的效果对比
5. **用户反馈闭环**：点赞/点踩 → 自动收集 bad case → 持续优化
6. **成本控制**：加入缓存层（相同城市 5 分钟内复用天气结果）+ 小模型优先（先用 qwen-turbo 意图分类，复杂问题再上 deepseek）
7. **安全护栏**：防止 LLM 给出危险建议（如暴雨天建议户外游泳）

**商业化路径：**
- 免费版：基础天气 + 通用建议
- 会员版：个性化知识库 + 家庭出行日历 + AI 行程规划
- B 端：对接旅行社/亲子乐园，提供 API

---

### Q21. LangChain/LangGraph 和 PocketFlow 的核心差异是什么？你如何向面试官解释为什么选 PocketFlow？

**参考答案：**

> 这是一个面试高频问题，回答好了直接加分。

**核心差异：**

| 维度 | LangChain/LangGraph | PocketFlow |
|------|-------------------|------------|
| **定位** | 全栈 LLM 应用框架 | 轻量级 Agent 编排框架 |
| **体积** | 重量级（数百个依赖） | 极简（纯 Python，约千行代码） |
| **学习曲线** | 陡峭（Chain/Agent/Tool 多层抽象） | 平缓（只有 Node/Flow 两个核心概念） |
| **灵活性** | 高度封装，自定义需深入源码 | 完全透明，prep/exec/post 三个方法 |
| **调试** | 复杂调用链追踪困难 | 代码即流程图，调试直观 |
| **生产** | 成熟生态（LangSmith 等） | 需自建配套 |

**为什么选 PocketFlow（面试话术）：**

> "这个项目的目标是**展示 Agent 的核心能力**而非工程框架的熟练度。PocketFlow 的 Node/Flow 模型足够简洁，面试官可以 5 分钟看懂编排逻辑。如果用 LangGraph，光 StateGraph 的定义、条件边的配置就要写一大堆模板代码，反而遮蔽了 Agent 设计的本质。"
>
> "从原理层面，PocketFlow 的 `prep → exec → post` 三段式和 LangGraph 的 `node + conditional_edge` 在抽象上是等价的。我选择更轻量的工具来聚焦 Agent 设计本身，同时也证明我不依赖某个特定框架——给任何一个框架我都能快速上手。"

**加分项：** 补充说明你在别的项目中用过 LangChain/LangGraph，从而证明"不是不会用，而是有选择地使用"。

---

### Q22. 如果 IntentAgent 的意图识别出现了错误（比如把"带孩子注意什么天气"判成了 chat），整个系统会怎样？如何防范？

**参考答案：**

**错误传播分析：**

```
用户: "带孩子注意什么天气"
        │
        ▼
  IntentAgent 误判 action="chat"  ← 错误发生
        │
        ▼
  ChatAgent 回复: "你可以问我城市天气哦"  ← 错误的用户体验
```

这是一个**致命级错误**——它会导致用户无法获得天气信息，被迫重新提问。

**多层防范策略：**

**第一层：Prompt 优化**
- 在 IntentAgent 的 prompt 中加入更多 few-shot 示例
- 对"带宝宝/带孩子/带老人"等高频场景增加重点标注

**第二层：二次确认机制**
```python
class IntentAgent(Node):
    def post(self, shared, prep_res, exec_res):
        action = exec_res.get("action", "chat")
        # 如果 LLM 不确定（低置信度），触发二次确认
        if exec_res.get("confidence", 1.0) < 0.7:
            # 同时走两条链路，取最优
            ...
        return action
```

**第三层：关键词兜底**
- 在 IntentAgent 的 post 中加入规则检查：如果问题包含"天气/温度/下雨/冷/热"等关键词但被判为 chat → 强制纠正为 weather
- 这比 LLM 的 prompt 更可靠（确定性规则）

**第四层：用户反馈闭环**
- ChatAgent 回复中加："如果您想问天气，请直接说城市名+天气"
- 记录意图识别修正 case → 持续优化 prompt

**第五层：评测集覆盖**
- 在 `eval_set.json` 中对边界 case 做充分覆盖（当前缺失）
- 每次改 prompt 后自动跑回归

---

### Q23. 项目中有一个 `utils/logger.py`（或被省略），日志体系在这个 Agent 系统中应该怎么设计？

**参考答案：**

虽然项目中 `utils/` 目录下没有显式的日志模块（实际只有 `llm.py` 和 `weather_client.py`），但生产环境的日志设计至关重要。

**推荐的分层日志体系：**

```
┌─────────────────────────────────────────────┐
│  应用日志（业务层）                           │
│  - 用户请求/回答摘要                         │
│  - 意图识别结果                              │
│  - Agent 路由路径                            │
├─────────────────────────────────────────────┤
│  链路追踪日志（Agent 间）                     │
│  - trace_id: 唯一请求标识                    │
│  - 每个 Agent 的 prep/exec/post 耗时         │
│  - MCP 调用耗时 + 重试次数                   │
├─────────────────────────────────────────────┤
│  基础设施日志（工具层）                       │
│  - LLM API 调用（模型/token 数/耗时）         │
│  - 天气 API 调用（城市/成功/失败/重试）        │
│  - RAG 检索（query/top-k/distance）          │
└─────────────────────────────────────────────┘
```

**关键设计：**

1. **trace_id 贯穿全链路**：从 API 请求 → IntentAgent → WeatherAgent → AdviceAgent，同一个 `trace_id` 串联所有日志，方便排查。

2. **关键节点的结构化日志**：
   ```python
   logger.info("agent_exec", extra={
       "trace_id": trace_id,
       "agent": "WeatherAgent",
       "cities": ["北京", "上海"],
       "latency_ms": 2340,
       "mcp_retries": 0,
       "fallback": False
   })
   ```

3. **分级告警**：
   - WARNING：单次 MCP 重试成功
   - ERROR：MCP 兜底触发（使用了假数据）
   - CRITICAL：连续 N 次 API 故障（可能需要切换备用数据源）

4. **成本监控**：
   - 每次请求的 token 消耗（DeepSeek）
   - 每日/每月 API 调用量和费用

---

### Q24. 如果天气 API（Open-Meteo）永久停止服务，你需要替换为另一个天气 API（如和风天气），需要改哪些文件？最小改动方案是什么？

**参考答案：**

**影响范围分析：**

| 文件 | 是否需要改动 | 说明 |
|------|------------|------|
| `utils/weather_client.py` | ✅ 核心改动 | 替换 API 实现 |
| `mcp/weather_mcp_server.py` | ❌ 不改 | 因为服务器只做 `tools/call` 路由，底层 `get_weather` 改了，服务器无感知 |
| `mcp/mcp_client.py` | ❌ 不改 | MCP 协议层不关心底层 API |
| `agents/weather_agent.py` | ❌ 不改 | 只调 `client.get_weather()`，不关心实现 |
| `agents/advice_agent.py` | ❌ 不改 | 消费 `shared["weather_data"]`，格式不变就不改 |

**最小改动方案：只改 1 个文件**

`utils/weather_client.py` 的 `get_weather()` 函数：

```python
def get_weather(city, retries=3, timeout=8):
    """新：使用和风天气 API"""
    for attempt in range(retries):
        try:
            # 和风天气 API 调用
            url = f"https://devapi.qweather.com/v7/weather/now?location={city}&key={API_KEY}"
            data = _get_json(url, timeout)
            return {
                "city": city,
                "temp": f"{data['now']['temp']}°C",
                "condition": data['now']['text'],
                "wind": f"{data['now']['windScale']}级",
            }
        except Exception as e:
            if attempt == retries - 1:
                return {"city": city, "temp": "N/A", ..., "fallback": True}
            time.sleep(0.5 * (attempt + 1))
```

**关键设计原则（由这个问题的答案体现）：**

这就是 **MCP 协议的价值所在**——工具提供者和工具消费者完全解耦。WeatherAgent 只知道通过 MCP 调 `get_weather`，不关心底层是 Open-Meteo 还是和风天气。替换只需改 `weather_client.py`，其他文件零改动，连 MCP Server 的 `TOOLS` 定义都不用改（schema 保持一致）。

---

## 快速应答速查表

| 问题类型 | 核心得分点 |
|---------|-----------|
| **架构设计** | 4 Agent 职责分离 + 关注点分离 + BatchNode 并行 |
| **MCP 协议** | JSON-RPC 2.0 + stdio + tools/list + tools/call + 手写 > SDK（展示理解深度） |
| **RAG** | BGE-M3 中文友好 + 纯 Python 余弦（小语料合适）+ 关键词回退（无 Key 可跑） |
| **记忆** | 双轨存储（城市频次 + 偏好标签）+ 跨会话复用 + repeat_rate 量化 |
| **容错** | 四重保护（重试/超时/限流/兜底）+ 不写兜底数据到记忆 |
| **降级** | 有 Key 用 DeepSeek / 无 Key 自动 mock → 离线可演示 |
| **评测** | 分层指标 + 每层可独立测 + 自动化输出 metrics.json |
| **工程化** | Docker + FastAPI + 纯 Python 少依赖 |
| **短板认知** | 主动指出 LLM 缺容错 + 偏好无衰减 + 评测集太小 + BatchNode 线程安全问题 |
